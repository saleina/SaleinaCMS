image: node:latest

before_script:
  - npm install --progress=false

cache:
  paths:
    - node_modules/

publish:
  stage: deploy
  only:
    - tags
  script:
    - npm run build
    - echo '//registry.npmjs.org/:_authToken=${NPM_TOKEN}'>.npmrc
    - npm publish 